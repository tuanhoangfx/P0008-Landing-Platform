# TOC portal (P0014) — landing + separate CRM tenant

# Landing dev
VITE_APP_VERSION=0.2.0

# TOC CRM iframe target
VITE_TOC_CRM_ORIGIN=http://127.0.0.1:3005
VITE_TOC_CRM_EMBED_REMOTE=1

# CRM gate — set PIN for /crm in production (empty = open in dev)
VITE_TOC_CRM_GATE_PIN=

# Vercel serverless — order intake (api/landing-order.mjs)
TOC_SUPABASE_URL=
TOC_SUPABASE_SERVICE_ROLE=
TOC_CRM_USER_ID=
