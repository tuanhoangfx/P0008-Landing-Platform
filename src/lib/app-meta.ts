/** HUB_SHELL_SCAFFOLD — product meta for headers + sidebar. */
import packageJson from "../../package.json";

/** App release label (keep in sync with package.json version). */
export const APP_VERSION = packageJson.version;
